[project]
name = "sanskriti-nvidia-chatterbox"
version = "0.2.0"
description = "Lightweight NVIDIA-hosted Chatterbox proxy for Sanskriti Explore"
requires-python = ">=3.11,<3.12"
dependencies = [
  "fastapi>=0.116.0",
  "nvidia-riva-client>=2.22.1",
  "python-dotenv>=1.1.0",
  "uvicorn[standard]>=0.35.0",
]

[tool.uv]
package = false
